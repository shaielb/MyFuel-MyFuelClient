package wrappers.tmp;

import java.util.HashMap;
import java.util.Map;

import annotations.AutoGenerated;
import application.Main;
import client.IClient;
import controls.MfImageView;
import db.entity.SystemUser;
import db.interfaces.IEntity;
import javafx.fxml.FXMLLoader;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.scene.image.ImageView;
import sceneswitch.SceneBase;

@AutoGenerated
public class MainMenuMyFuelScreen extends SceneBase {

	public MainMenuMyFuelScreen(ISceneSwitcher sceneSwitcher, IClient client) throws Exception {
		super(sceneSwitcher, client);
		initialize();
	}

	public void initialize() throws Exception {
		Parent root = FXMLLoader.load(Main.class.getResource("MainMenuMyFuelScreen.fxml"));
		_scene = new Scene(root);

		//scene switchers
		MfImageView aboutUsScreenControl = new MfImageView((ImageView) _scene.lookup("#scene$AboutUsScreen"));
		aboutUsScreenControl.addEvent((event) -> { _switcher.switchScene("AboutUsScreen"); });

		MfImageView signUpPersonalDetailsScreenControl = new MfImageView((ImageView) _scene.lookup("#scene$SignUpPersonalDetailsScreen"));
		signUpPersonalDetailsScreenControl.addEvent((event) -> { _switcher.switchScene("SignUpPersonalDetailsScreen"); });

		MfImageView logInScreenControl = new MfImageView((ImageView) _scene.lookup("#scene$LogInScreen"));
		logInScreenControl.addEvent((event) -> { _switcher.switchScene("LogInScreen"); });


	}
	private Map<IEntity, Map<String, String>> prepareQuery(SystemUser systemUser) {
		Map<IEntity, Map<String, String>> map = new HashMap<IEntity, Map<String, String>>();
		
		Map<String, String> queryMap = new HashMap<String, String>();
		
		map.put(systemUser, queryMap);
		return map;
	}
}
