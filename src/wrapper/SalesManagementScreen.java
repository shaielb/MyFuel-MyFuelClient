package wrapper;

import javafx.scene.layout.BorderPane;
import javafx.fxml.FXMLLoader;
import adapter.base.ControlAdapter;
import java.util.HashSet;
import widgets.table.Table;
import controls.*;
import annotations.AutoGenerated;
import java.util.HashMap;
import javafx.scene.Parent;
import sceneswitch.SceneBase;
import table.MfTable;
import widgets.table.*;
import javafx.scene.Scene;
import javafx.scene.control.*;
import action.*;
import db.interfaces.IEntity;
import javafx.scene.image.ImageView;
import java.util.Set;
import javafx.scene.text.Text;

@AutoGenerated
public class SalesManagementScreen extends SceneBase {

	public SalesManagementScreen(ISceneSwitcher sceneSwitcher) {
		super(sceneSwitcher);
	}

	public void initialize() {
		Parent root = FXMLLoader.load(getClass().getResource("SalesManagementScreen.fxml"));
		_scene = new Scene(root);

		//entities instantiation


		//controls instantiation


		//initializations
		MfHyperlink maimMenuMarketingScreenControl = new MfHyperlink((Hyperlink) _scene.lookup("#scene$MaimMenuMarketingScreen"));
		maimMenuMarketingScreenControl.addEvent((event) -> { _switcher.switchScene("MaimMenuMarketingScreen"); });
		MfImageView maimMenuMarketingScreenControl = new MfImageView((ImageView) _scene.lookup("#scene$MaimMenuMarketingScreen"));
		maimMenuMarketingScreenControl.addEvent((event) -> { _switcher.switchScene("MaimMenuMarketingScreen"); });
		BorderPane companySalesBp = (BorderPane) _scene.lookup("#uitable$editable$single$company_sales");
		Table<CompanySales> companySalesTableWrapper = new Table<CompanySales>();
		MfTable<CompanySales> companySalesTable = new MfTable<CompanySales>(CompanySales.class);
		companySalesTable.setEditable(true);
		MfSingleCapability<CompanySales> companySalesSingleCapability = new MfSingleCapability<CompanySales>();
		companySalesTableWrapper.addCapability(companySalesSingleCapability);
		companySalesTableWrapper.setTable(companySalesTable);
		companySalesBp.setCenter(companySalesTable.getInstance());
		MfImageView companySalesControl = new MfImageView((ImageView) _scene.lookup("#action$update$company_sales"));
		ActionControl updateAction = new ActionControl();
		updateAction.setControl(companySalesControl);
		UpdateCapability companySalesUpdateCapability = new UpdateCapability();
		Set<IEntity> companySalesUpdateEntities = new HashSet<IEntity>();
		companySalesUpdateCapability.setEntities(companySalesUpdateEntities);
		updateAction.addCapability(companySalesUpdateCapability);
		updateAction.setCallback((response) -> {});
		MfImageView saleReportScreenControl = new MfImageView((ImageView) _scene.lookup("#scene$SaleReportScreen"));
		saleReportScreenControl.addEvent((event) -> { _switcher.switchScene("SaleReportScreen"); });

		//fields initializations


		//grouping

	}

}
